.container.py-5.h-100
  .row.d-flex.justify-content-center.align-items-center.h-100
    .col-12.col-md-8.col-lg-6.col-xl-5
      .card.bg-dark.text-white{ style: "border-radius: 1rem;" }
        .card-body.p-5.text-center
          .mb-md-5.mt-md-4.pb-4
            %h2.fw-bold.mb-2.text-uppercase
              Забули пароль?
            %p.text-white-50.mb-5
              Будь ласка, ведіть електронну адресу для відновлення пароля!
            = form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post }) do |f|
              .form-outline.form-white.mb-4
                = f.email_field :email, autofocus: true, autocomplete: "email", placeholder: "Електронна адреса", class: "form-control form-control-lg"
              = f.submit "Надіслати лист", class: "btn btn-outline-light btn-lg px-5"
          %div
            %p.mb-1
              Згадали пароль?
              = link_to "Увійти", new_session_path(resource_name), class: "text-white-50 fw-bold"
            %p.mb-0
              Не маєте облікового запису?
              = link_to "Зареєструватись", new_registration_path(resource_name), class: "text-white-50 fw-bold"
